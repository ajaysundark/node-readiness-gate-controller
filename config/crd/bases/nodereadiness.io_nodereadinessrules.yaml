---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: nodereadinessrules.nodereadiness.io
spec:
  group: nodereadiness.io
  names:
    kind: NodeReadinessRule
    listKind: NodeReadinessRuleList
    plural: nodereadinessrules
    shortNames:
    - nrr
    singular: nodereadinessrule
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeReadinessRule is the Schema for the nodereadinessrules API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NodeReadinessRule
            properties:
              conditions:
                description: Conditions specifies a list of node conditions that must
                  be satisfied for this rule to be applied
                items:
                  description: ConditionRequirement specifies a required node condition
                    and its expected status
                  properties:
                    requiredStatus:
                      description: RequiredStatus is the expected status of the condition
                        (True, False, or Unknown)
                      type: string
                    type:
                      description: Type is the name of the node condition to check
                      type: string
                  required:
                  - requiredStatus
                  - type
                  type: object
                type: array
              dryRun:
                description: DryRun if true, shows what would happen without actually
                  applying taints
                type: boolean
              enforcementMode:
                description: 'EnforcementMode determines how the rule is enforced:
                  either once during node bootstrap or continuously'
                type: string
              gracePeriod:
                description: GracePeriod specifies how long to wait before applying
                  the taint after conditions are not met
                type: string
              nodeSelector:
                description: NodeSelector is a label selector to filter which nodes
                  this rule applies to
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              taint:
                description: Taint specifies the taint to be applied to nodes that
                  do not satisfy the conditions
                properties:
                  effect:
                    description: Effect indicates what effect this taint has (NoSchedule,
                      PreferNoSchedule, or NoExecute)
                    type: string
                  key:
                    description: Key is the taint key to be applied
                    type: string
                  value:
                    description: Value is an optional value associated with the taint
                    type: string
                required:
                - effect
                - key
                type: object
            required:
            - conditions
            - enforcementMode
            - taint
            type: object
          status:
            description: status defines the observed state of NodeReadinessRule
            properties:
              appliedNodes:
                description: AppliedNodes is a list of nodes where this rule has been
                  successfully applied
                items:
                  type: string
                type: array
              completedNodes:
                description: CompletedNodes lists nodes that have completed bootstrap-mode
                  evaluation
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the rule's current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dryRunResults:
                description: DryRunResults contains the analysis results when running
                  in dry-run mode
                properties:
                  affectedNodes:
                    description: AffectedNodes is the count of nodes that would be
                      affected
                    type: integer
                  riskyOperations:
                    description: RiskyOperations is the count of operations that might
                      impact workload availability
                    type: integer
                  summary:
                    description: Summary provides a human-readable summary of the
                      dry run analysis
                    type: string
                  taintsToAdd:
                    description: TaintsToAdd is the count of taints that would be
                      added
                    type: integer
                  taintsToRemove:
                    description: TaintsToRemove is the count of taints that would
                      be removed
                    type: integer
                required:
                - affectedNodes
                - riskyOperations
                - summary
                - taintsToAdd
                - taintsToRemove
                type: object
              failedNodes:
                description: FailedNodes lists nodes where rule application has failed,
                  with failure details
                items:
                  description: NodeFailure represents a failure to apply the rule
                    to a specific node
                  properties:
                    lastUpdated:
                      description: LastUpdated is when this failure was last updated
                      format: date-time
                      type: string
                    message:
                      description: Message is a detailed explanation of the failure
                      type: string
                    nodeName:
                      description: NodeName is the name of the node where the failure
                        occurred
                      type: string
                    reason:
                      description: Reason is a brief reason for the failure
                      type: string
                  required:
                  - lastUpdated
                  - nodeName
                  - reason
                  type: object
                type: array
              nodeEvaluations:
                description: NodeEvaluations contains detailed evaluation results
                  for each affected node
                items:
                  description: NodeEvaluation represents the evaluation state of a
                    single node
                  properties:
                    conditionResults:
                      description: ConditionResults contains the evaluation results
                        for each condition
                      items:
                        description: ConditionEvaluationResult represents the evaluation
                          of a single condition requirement
                        properties:
                          currentStatus:
                            description: CurrentStatus is the actual status of the
                              condition on the node
                            type: string
                          missing:
                            description: Missing indicates whether the condition is
                              present on the node
                            type: boolean
                          requiredStatus:
                            description: RequiredStatus is the status required by
                              the rule
                            type: string
                          satisfied:
                            description: Satisfied indicates whether the condition
                              requirement is met
                            type: boolean
                          type:
                            description: Type is the name of the evaluated condition
                            type: string
                        required:
                        - currentStatus
                        - missing
                        - requiredStatus
                        - satisfied
                        - type
                        type: object
                      type: array
                    lastEvaluated:
                      description: LastEvaluated is the timestamp of the most recent
                        evaluation
                      format: date-time
                      type: string
                    nodeName:
                      description: NodeName is the name of the evaluated node
                      type: string
                    taintStatus:
                      description: TaintStatus indicates whether the taint is Present,
                        Absent, or Unknown on the node
                      type: string
                  required:
                  - conditionResults
                  - lastEvaluated
                  - nodeName
                  - taintStatus
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed NodeReadinessRule
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
